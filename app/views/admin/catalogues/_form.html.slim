.card
  .card-header
    h3.card-title New Catalogue
  = simple_form_for [:admin, catalogue] do |f|
    .card-body
      .form-group
        = f.input :name, input_html: { class: "form-control" }
    .card-footer
      = f.button :submit, class: "btn btn-success"